- [x] Create a Go utility to correct source boilerplate copyright years and authors from per-file Git history, then verify it against a temp clone of `~/ibackup` without committing or pushing there.
  - Added `boilerplate` as a Go CLI with Git-history scanning, year range formatting, author deduplication by name, and supported Go/Python/Nextflow boilerplate rewrites.
  - Added GoConvey tests covering year formatting, all supported comment styles, and a real temporary Git repo with per-file history.
  - Verified with an uncommitted temp clone at `.tmp/agent/ibackup-boilerplate-20260622100812`.
  - Moved the executable entrypoint to repo-root `main.go` so `go run .` works.
