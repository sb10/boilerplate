- [x] `git -C .tmp/agent/ibackup-boilerplate-20260622100812/ diff` shows bad email addresses, and `go run . -repo .tmp/agent/ibackup-boilerplate-20260622100812/ -write` doesn't change that fact. Reset the repo if needed. If it still gives bad email addresses (noreply.github.com), use the bugfix skill to fix it so the previous request actually works. When considering existing author emails in the repo, pick the most commonly used one.
  - Added a Git-backed regression test covering most-common header email selection, current-file real email priority, shared GitHub noreply identity inference, noreply omission when no real email is known, and normal commit-email fallback.
  - Reworked author email resolution to count recognized header emails, prefer real addresses, map shared GitHub noreply identities to known real header emails, and never write `noreply.github.com` addresses into generated headers.
  - Verified the temp clone after reset and `go run . -repo .tmp/agent/ibackup-boilerplate-20260622100812 -write`; the generated diff has no `noreply.github.com` matches.
