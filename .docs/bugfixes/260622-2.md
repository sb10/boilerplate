- [x] Capture all existing author email addresses, then use those when adding known authors to new files. Fallback to the commit email address only for authors who have never been included in a header.
  - Added a repo-wide scan of recognized headers so known author emails can be reused when rewriting selected files.
  - Kept current-file header emails as the highest priority and commit emails as the fallback for authors never seen in a header.
  - Added a Git-backed regression test covering known header email reuse and unknown author commit-email fallback.
